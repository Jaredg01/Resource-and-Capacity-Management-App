/**
 * ActivityCategory enumeration
 */
export const ActivityCategory = {
  VACATION: 0,
  BASELINE: 1,
  STRATEGIC: 2,
  DISCRETIONARY_PROJECT: 3
};

// Freeze the object to prevent modifications
Object.freeze(ActivityCategory);
